# ProofToAct claims ledger

Every public claim must remain **unverified** until the listed artifact exists
and its acceptance condition passes. Synthetic scenarios are always labeled
synthetic and non-operational.

| Claim | Current status | Required artifact | Acceptance condition |
| --- | --- | --- | --- |
| CockroachDB Distributed Vector Indexing ranks a database-produced snapshot of admissible memory candidates. | DVI mechanics and admissibility verified separately; integrated snapshot plus exact drill/top-evidence binding source-tested locally; fresh live plan and exclusion receipt pending | Historical [`evidence/gate1-vector-2026-07-29.md`](evidence/gate1-vector-2026-07-29.md), [`evidence/gate1-authority-2026-07-30.md`](evidence/gate1-authority-2026-07-30.md), and a new integrated receipt bound to the final implementation | The historical cloud plan proves real DVI mechanics and the authority receipt proves deterministic admissibility, but neither proves the named index ranks the full gate output. The local candidate binds its top-ranked evidence to one synthetic drill without disclosing fixture identifiers. The source path now consumes database-authorized DVI proposal identities and the exact selected-evidence digest, but no provider-backed receipt yet proves the live DVI-to-AWS handoff. Acceptance still requires a fresh mixed-fixture snapshot, adversarial exclusions, and `EXPLAIN (VERBOSE)` naming the snapshot index with exact tenant/retrieval prefix spans. |
| CockroachDB Managed MCP can inspect sanitized recovery state without transferring operational authority. | Gate One verified; live signed-out fixed-read transport verified on a synthetic signed context | [`evidence/gate1-recovery-broker-2026-07-30.md`](evidence/gate1-recovery-broker-2026-07-30.md) and [`evidence/live-judge-provider-read-2026-08-19.md`](evidence/live-judge-provider-read-2026-08-19.md) | The live parameter-free AWS Lambda route completed one Managed MCP initialize, notification, fixed query, and close sequence, required exactly one pinned P-256-signed historical context row, and returned a minimized receipt with `authorityTransferred: false` and `requiresFreshAuthorization: true`. This verifies the current transport and signed-row read, not current authority or the complete Gate Two drill. |
| SQL admissibility precedes semantic ranking and authority. | Authority ordering verified; run-bound admissible-snapshot ranking source-tested locally; fresh live integrated receipt pending | [`evidence/gate1-authority-2026-07-30.md`](evidence/gate1-authority-2026-07-30.md), historical [`evidence/gate1-vector-2026-07-29.md`](evidence/gate1-vector-2026-07-29.md), and a new integrated receipt | Signed provenance, validity, revocation, scope, and conflict checks already deny authority without changing the fence or creating an outbox. The local candidate now binds its top-ranked result to an exact synthetic drill. The new live receipt must prove those same gates materialize the ranked snapshot; current authorization must still recheck admissibility because a snapshot can become stale. |
| Concurrent contenders produce one authority winner. | Historical Gate One verified, 100 × 50 live races; Final23 did not repeat this drill | [`evidence/gate1-authority-2026-07-30.md`](evidence/gate1-authority-2026-07-30.md) | The historical accepted receipt records exactly one lease, winning receipt, fence increment, and outbox intent for every race; all 49 denials remained durable and sticky. This is not a Final23 contention result. |
| Process death around COMMIT never triggers blind replay in the synthetic authority gate. | Historical Gate One verified, 100 runs per boundary; Final23 did not repeat this drill | [`evidence/gate1-ambiguity-2026-07-30.md`](evidence/gate1-ambiguity-2026-07-30.md) | The historical accepted receipt records only empty, complete, or fail-closed unknown state across the before-COMMIT, COMMIT-dispatched, and acknowledged-COMMIT boundaries, with no partial transaction or blind retry. This is not a Final23 ACK-loss result. |
| The approved 100+1 high-water target binds 100 deterministic offline specifications plus one exact-release provider-backed integrated drill. | Partial; the recomputing offline receipt passes 100 unique runs and 1,100 invariant evaluations with zero violations, while the +1 provider execution and acceptance review remain pending | [`docs/FULL_DRILL_EVIDENCE.md`](docs/FULL_DRILL_EVIDENCE.md), [`evidence/local-full-drill-100-2026-08-04.json`](evidence/local-full-drill-100-2026-08-04.json), [`evidence/final23-local-cockroach-v26-2-x86-2026-08-14.json`](evidence/final23-local-cockroach-v26-2-x86-2026-08-14.json), and a future `tideproof.highwater-drill-live.v1` receipt | The local receipt satisfies only the offline half. The one accepted provider run must come from the exact clean release and bind one real admissible-snapshot DVI query to a genuinely overlapping two-Lambda race, exact replay, changed-input denial, exact-winner Managed MCP recovery, bounded cleanup, and zero invariant violations. The source now derives one canonical recovery attempt from the tenant, caller, source digest, and committed source time. Before publication it create-only persists and verifies an owner-only canonical envelope containing the exact normalized signed recovery bundle; a later invocation with the same unsigned bundle must reuse the first randomized P-256 signature bytes. Reuse resyncs the validated file and parent and proves the current pathname, but explicitly does not claim that invocation observed the original create/link protocol. Current bytes are source-bound, but a real restart and crash continuity remain unproven. One negotiated Managed MCP session plus canonical request, response, and result digests are bound through close. Local source-control helpers write the signed bundle and raw component bundle only to an approved owner-only root outside the Git checkout and bind the currently reread bytes, but their unkeyed receipts do not independently prove the historical write protocol, durable retention, or crash continuity. Those are fail-closed source controls, not independent provider signatures or live evidence. The repaired source keeps every contender, controller, worker, loser, and reconciler free of the MCP bearer. Separate claim, begin, redeem, finalize, activation, terminalization, and resolve-only database roles move the global row to `EXECUTING` before a local execution grant exists; the worker then creates a one-time execution fence and calls a nonce-bound Unix socket. The persistent systemd-isolated provider-operation broker is provider-key-isolated: it receives redeem/finalize database URLs and the sealed execution capability but never the MCP bearer. It atomically commits `CREDENTIAL_REDEEMED`, requests fresh database activation, and admits a fixed one-shot provider exchange only after exact `READY` receipt matching. That distinct exchange UID alone receives the bearer; after `PROCEED_ONCE` it rechecks activation freshness, creates a consumption fence, performs the fixed provider sequence, durably records the result, and only then returns callback success. A separate database-time terminalizer converges expiry and ambiguity without provider replay. The reconciler remains resolve-only, and the migration drops the legacy generic transition. Seven distinct service identities isolate the runtime, dispatch broker, operation broker, activation, provider exchange, terminalizer, and reconciler. The Linux/systemd first-exec boundary creates a clean environment before dynamic Perl, descriptor-executes verified Node, and binds a deterministic root-stage schema v5 installer plus an independently implemented verifier. The v9 build contains twelve content-addressed ESM runtime bundles and 24 outputs; the root stage binds fifteen runtime files (the twelve bundles, launcher, canonical manifest, and pinned Node executable) across fourteen systemd units. A count-bound 20/20 local stress receipt now repeats a real two-OS-process broker race against a shared file-backed fake global control; it is not a real two-host CockroachDB or provider receipt. Final23 adds only a fresh-zero local QEMU-TCG x86-64 CockroachDB CCL v26.2.0 primary-capability-boundaries pass with typed-definer access, direct-DML/cross-role denials, simple recovery resolution, DVI proposal-identity negatives, wrong-digest rejection, one protected-effect insert, and sequential exact replay. It did not run final-head 50-session contention, held-expiry waits, simultaneous four-key uniqueness conflicts, ambiguity/ACK-loss, or integrated DVI plan/exclusions. No provider-hosted or external CockroachDB receipt, accepted PID1 isolation receipt, or accepted root-stage receipt exists yet. The source path now consumes database-authorized DVI proposal identities and the exact selected-evidence digest, but no provider-backed receipt yet proves the live DVI-to-AWS handoff. The runner emits only a non-accepting `tideproof.highwater-drill-live-candidate.v2`; signed deployment attestation, independent journal and private-evidence retention/recomputation review, provider-backed restart-stable signed-bundle reuse, a real cross-host one-grant database transition, root-stage and PID1 execution, and crash-safe single-call recovery remain mandatory. Historical component receipts cannot substitute. |
| A successor recovers context without inheriting authority. | Gate One verified, synthetic broker | [`evidence/gate1-recovery-broker-2026-07-30.md`](evidence/gate1-recovery-broker-2026-07-30.md) | Recovery exposes only signed, fresh, sanitized context; an unbound principal never reaches MCP; authority must be freshly acquired through a separate capability. |
| Stale fencing tokens cannot produce a protected synthetic effect. | Historical Gate One broad fence matrix verified; Final23 exact-version local digest/replay subset passed | [`evidence/gate1-authority-2026-07-30.md`](evidence/gate1-authority-2026-07-30.md) and [`evidence/final23-local-cockroach-v26-2-x86-2026-08-14.json`](evidence/final23-local-cockroach-v26-2-x86-2026-08-14.json) | The historical accepted receipt covers stale, future, expired, wrong-actor, wrong-tenant, wrong-incident, wrong-resource, wrong-run, and 50-way replay behavior. Final23 narrowly proves on local CockroachDB CCL v26.2.0 that a wrong digest inserts zero rows, one current valid call inserts one protected row, sequential exact replay inserts zero more, a later wrong digest inserts zero more, and direct table insertion is denied with SQLSTATE `42501`. It does not prove held-wait, simultaneous four-key conflicts, final-head contention, provider behavior, or an exactly-once external effect. |
| ProofToAct fails closed when its memory or recovery path is unavailable. | Local broker behavior verified; deployed outage proof pending | Database and MCP outage receipts | State remains `UNKNOWN_DO_NOT_ACT`; no context is released without committed pre-read and terminal audit events; no authorization bypass exists. |
| A signed-out AWS judge surface can remain separate from proposal and authority capabilities. | Narrow fixed-read judge route live; broader Gate Two surface remains separately gated | [`evidence/live-judge-provider-read-2026-08-19.md`](evidence/live-judge-provider-read-2026-08-19.md), `docs/AWS_GATE2.md`, exact-head demo artifact, API route inventory, role audit, and signed-out human review | The current public route accepts only one parameter-free `GET`, invokes immutable Lambda version 3, performs one pinned Managed MCP read, returns a minimized no-store receipt, and transfers no authority. Local tests separately execute the broader Gate Two builder in an isolated exact-commit worktree, reject project path escape, expose only enumerated routes through numeric Lambda versions, and keep proposal and authority paths capability-separated. The live fixed-read receipt does not prove the broader Bedrock, KMS, IAM-denial, authority-race, or deployment-attestation gates. |
| A bounded AWS proposal path can be deployed without giving the model authority. | Hardened local Gate Two candidate verified; live AWS proof pending | `docs/AWS_GATE2.md` and future AWS evidence receipt plus reconciled API access log | Local tests validate one exact Nova request schema, proposal-only output, exact artifact hashes, independent exact-key `kms:GetPublicKey`, signed key ARN/fingerprint bindings, local P-256 verification, dedicated exact-route caller policy, and fail-closed adversarial cases. A receipt alone does not prove API traversal; live acceptance requires a matching private API access-log record and direct-Lambda denials. Live Bedrock, KMS, IAM, API Gateway, and CloudFormation behavior remain unverified until deployed evidence exists. |
| Two AWS Lambda contenders can overlap while CockroachDB commits one fresh authority winner. | Local handler, IAM, transaction, exact-caller evidence binding, replay/changed-input negatives, durable-proof, and verifier candidate tested; no live AWS/DB race claim | `docs/AWS_GATE2.md`, exact-head Authority artifact, private Lambda invocation and STS receipts, CockroachDB receipts, live IAM/secret audit, and sanitized `tideproof.aws-authority-race-receipt.v7` | The local candidate admits only two named contenders for one configured synthetic run and race, requires both durable decisions to carry one shared DVI authority-evidence binding and selected evidence identity, derives operation-bearing fields outside the model, invokes one exact numeric Authority version, rejects endpoint/profile/proxy/CA evidence redirection, binds the exact AWS account, CloudFormation-managed role, and precommitted observed STS caller ARN/UserId triple before invocation, reads one exact project secret, calls only typed `SECURITY DEFINER` functions, requests `SERIALIZABLE`, retries only pre-commit `40001`, and reconciles ambiguous COMMIT. It then requires the winner's exact operation replay and a database-denied changed input under that operation before the later durable observation. The changed-input probe first revalidates the exact original durable receipt inside the same serializable transaction, recognizes only the spend call's typed digest mismatch, and rolls back without committing if the probe unexpectedly returns. The race receipt also requires five distinct Lambda invocation IDs and five distinct AWS Invoke request IDs. `GetCallerIdentity` does not prove a globally unique AssumeRole issuance when a session name can be reused. Two overlapping contender responses are insufficient: a later serializable read-only observation must bind the exact configured active run plus the same operation and request digests and find exactly two terminal receipts, one winner, one denial that observed that winner at the same fence, one matching outbox, the winning holder and fence, no pending receipt, and zero protected effects. Injected local clients and the unrun deployment-attestation contract do not prove Lambda concurrency, Secrets Manager, IAM, STS, CockroachDB reachability, or live durable state. |

The repository does not claim invention of provenance-aware memory, temporal
memory, fencing, replay protection, or exactly-once real-world effects.

The current claim-bearing README, browser, scenario, technical-boundary,
video-script, contest, and submission surfaces are hash-bound by
`RELEASE_CLAIMS_MANIFEST.json`. `npm run claims:verify` rejects drift from the
reviewed pending state but does not prove truth, authorize publication, or
replace the exact-release private human review.
