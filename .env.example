# ProofToAct never commits live credentials.
# Local development may inject DATABASE_URL from an OS credential store.
# Never place an OpenClaw OAuth credential here.
DATABASE_URL=
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=
