# Local proof does not require credentials.
#
# Future cloud integrations must use project-owned credentials only.
# Never place an OpenClaw OAuth credential here.
COCKROACH_DATABASE_URL=
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=
